{
  "name": "codex-legalist",
  "version": "1.0.0",
  "private": true,
  "description": "Autonomous Courtroom Simulation — Frontend",
  "scripts": {
    "lint": "eslint static/ --fix",
    "format": "prettier --write static/ index.html",
    "format:check": "prettier --check static/ index.html"
  },
  "devDependencies": {
    "eslint": "^9.0.0",
    "prettier": "^3.5.0"
  }
}
